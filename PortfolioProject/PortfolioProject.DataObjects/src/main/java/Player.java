public class Player {
    private String playerName;
    private Character activeCharacter;

    private Enums.itemArmorType itemArmorType;

    @Override
    public String toString() {
        //if (activeCharacter == null)
            return playerName;
//        else
//            return playerName + " " + activeCharacter.getEntityName();
    }
}
